# ============================================
# EduStore Backend - Environment Template
# ============================================
<<<<<<< HEAD
# Copy this file to .env and fill in your values
# NEVER commit .env to git - keep it in .gitignore

# Database (NeonDB PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require&channel_binding=require

# Brevo Email API
# CRITICAL: EMAIL_FROM must be verified in Brevo Dashboard
# Steps: Brevo Dashboard → Settings → Senders & IPs → Add Sender → Verify Email
BREVO_API_KEY=your_brevo_api_key_here
EMAIL_FROM=your-verified-email@domain.com
EMAIL_FROM_NAME=EduStore

# SMTP (Legacy - for local development only)
SMTP_USER=your-smtp-user@gmail.com
SMTP_PASS=your-app-password

# JWT Secret Key
# Generate a strong random key for production
SECRET_KEY=your-secret-key-here

# Redis Cache (Upstash)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# Cloud Storage (Cloudinary)
STORAGE_PROVIDER=cloudinary
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Chat Microservice
# Use production URL for Render deployment, localhost for local dev
CHAT_SERVICE_URL=https://your-chat-service.up.railway.app

# Frontend URL for CORS
# Use production URL for Render deployment, localhost for local dev
FRONTEND_URL=https://your-frontend.vercel.app
=======
# Copy this file to .env and fill in the values

# Mode: development | production
ENVIRONMENT=development

# Database (NeonDB PostgreSQL or Local)
# Example: postgresql://user:pass@host:port/db
DATABASE_URL=

# Brevo Email API (Primary for individual emails)
BREVO_API_KEY=
EMAIL_FROM=
EMAIL_FROM_NAME=EduStore

# SMTP (Legacy - for local development fallback only)
SMTP_USER=
SMTP_PASS=

# JWT Secret Key
# Use `python -c "import secrets; print(secrets.token_urlsafe(64))"` to generate
SECRET_KEY=

# Redis Cache (Upstash)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Cloud Storage (Cloudinary)
STORAGE_PROVIDER=cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Chat Microservice 
# LOCAL: http://localhost:3000
# PRODUCTION: https://your-chat-service.railway.app
CHAT_SERVICE_URL=http://localhost:3000

# Frontend URL for CORS
# LOCAL: http://localhost:5173
# PRODUCTION: https://your-frontend.vercel.app
FRONTEND_URL=http://localhost:5173
>>>>>>> 5cfc842 (new version of it)
